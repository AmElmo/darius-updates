# Darius Update Channel

This directory is published via GitHub Pages and hosts `appcast.xml` for Darius's Sparkle auto-update mechanism. The release CI workflow in the private source repo publishes releases here.
